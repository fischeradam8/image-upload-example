import { Theme } from "styled-system";

const theme: Theme = {
  colors: {
    primary: "#c7ea46",
    border: "#979797",
    neutral: "#464646",
    contrastText: "white",
  },
};

export default theme;
